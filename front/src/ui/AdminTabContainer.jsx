export const AdminTabContainer = ({ children }) => {
  return (
    <div className="flex flex-col h-dvh items-center justify-center w-full p-8 gap-4 overflow-hidden">
      {children}
    </div>
  )
}
